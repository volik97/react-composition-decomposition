// компонент который рендерит блоки из рекомендаций

export default function item() {
  return (
    <div className="item">
        {/*
        <div className={'item__title'}><a href={props.url}>{props.title}</a></div>
			{children}
        < */}
    </div>
  )
}
