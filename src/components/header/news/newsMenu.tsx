
// Компонент принимающий в props массив тегов ['сейчас в сми', 'в германии', ...]
// рендерит их в список c handleClick, который меняет newsList
// 
export default function newsMenu() {
  return (
    <div>newsMenu</div>
  )
}
