// Компонент отвечающий за рендер списка новостей 
//

export default function newsList() {
  return (
    <ul>
        {/* Массив объектов {icon: url, text: string, href: url}.map(item => 
            <li>
                <a href={item.href})> <img src={item.icon} alt=''/> item.text</a>
            </li>
        */}
    </ul>
  )
}
